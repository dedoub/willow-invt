// 증빙 묶음(evidence bundle) 생성 — Task 5에서 구현.
// buildBundle: 정산의 업무기록·증빙·문서·인보이스·현금 스냅샷으로 번들 콘텐츠를 만든다.
// registerBundle: 만든 번들을 법인 서류함(evidence_bundle 문서)에 등록하고 정산에 연결한다.

export async function buildBundle(_db, _ref) {
  throw new Error('bundle not implemented')
}

export async function registerBundle(_db, _ref, _bundle) {
  throw new Error('bundle not implemented')
}
