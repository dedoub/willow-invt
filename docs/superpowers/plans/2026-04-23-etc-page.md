# ETC Linear Page Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build the ETC management page at `/willow-investment/etc` with Linear design system, matching all features from the legacy `/etf/etc` page.

**Architecture:** Follows the Akros page pattern — Linear design tokens (`t.*`), `LCard`/`LSectionHead`/`LBtn`/`LIcon` primitives, grid layout with stats+products (2/3) + invoices (1/3) + wiki (2/3) + email (1/3). Reuses existing lib functions (`supabase-etf.ts`, `invoice/*`) and shared components (`WikiList`, `EmailBlock`). All API routes already exist — no backend changes needed.

**Tech Stack:** Next.js App Router, React (client components), inline styles with Linear design tokens, Supabase (data), Gmail API (email/invoice send).

---

## File Structure

```
Create:
  src/app/willow-investment/(linear)/etc/page.tsx
  src/app/willow-investment/(linear)/etc/_components/stats-block.tsx
  src/app/willow-investment/(linear)/etc/_components/product-block.tsx
  src/app/willow-investment/(linear)/etc/_components/product-dialog.tsx
  src/app/willow-investment/(linear)/etc/_components/document-panel.tsx
  src/app/willow-investment/(linear)/etc/_components/invoice-block.tsx
  src/app/willow-investment/(linear)/etc/_components/invoice-dialog.tsx
  src/app/willow-investment/(linear)/etc/_components/invoice-send-dialog.tsx
  src/app/willow-investment/(linear)/etc/_components/wiki-block.tsx

Modify:
  src/app/willow-investment/_components/linear-sidebar.tsx (add ETC link)
  src/app/willow-investment/_components/linear-skeleton.tsx (add EtcSkeleton)
  src/components/layout/layout-wrapper.tsx (add to LINEAR_ROUTES)
```

---

### Task 1: Route scaffolding — sidebar link, skeleton, empty page

**Files:**
- Modify: `src/app/willow-investment/_components/linear-sidebar.tsx`
- Modify: `src/app/willow-investment/_components/linear-skeleton.tsx`
- Modify: `src/components/layout/layout-wrapper.tsx`
- Create: `src/app/willow-investment/(linear)/etc/page.tsx`

**Context:** The sidebar CLIENTS array already has ETC (`{ id: 'etc', name: 'ETC', tag: 'ETF Platform', dot: '#1F4E79' }`), but only `akros` has an href. LINEAR_ROUTES is at line 13 of layout-wrapper.tsx.

- [ ] **Step 1: Update sidebar to link ETC**

In `linear-sidebar.tsx`, find the line (around line 89):
```typescript
const href = c.id === 'akros' ? '/willow-investment/akros' : undefined
```
Change to:
```typescript
const href = c.id === 'akros' ? '/willow-investment/akros'
  : c.id === 'etc' ? '/willow-investment/etc'
  : undefined
```

- [ ] **Step 2: Add to LINEAR_ROUTES**

In `layout-wrapper.tsx`, find `LINEAR_ROUTES` and add `/willow-investment/etc`:
```typescript
const LINEAR_ROUTES = ['/willow-investment/mgmt', '/willow-investment/invest', '/willow-investment/wiki', '/willow-investment/ryuha', '/willow-investment/akros', '/willow-investment/etc']
```

- [ ] **Step 3: Add EtcSkeleton**

In `linear-skeleton.tsx`, add after `AkrosSkeleton`:
```tsx
export function EtcSkeleton() {
  return (
    <div style={{ display: 'flex', flexDirection: 'column', gap: 14 }}>
      {/* Stats + Invoices */}
      <div style={{ display: 'grid', gridTemplateColumns: '2fr 1fr', gap: 14 }}>
        <CardSkel>
          <Bone w={100} h={8} />
          <Bone w={80} h={14} style={{ marginTop: 6 }} />
          <div style={{ display: 'flex', gap: 12, marginTop: 12 }}>
            <Bone w={120} h={46} r={6} />
            <Bone w={120} h={46} r={6} />
            <Bone w={120} h={46} r={6} />
          </div>
        </CardSkel>
        <CardSkel pad={0}>
          <div style={{ padding: '16px 16px 10px' }}>
            <Bone w={80} h={8} />
            <Bone w={80} h={14} style={{ marginTop: 6 }} />
          </div>
          <div style={{ padding: '0 16px 16px' }}>
            {Array.from({ length: 6 }).map((_, i) => (
              <Bone key={i} h={28} style={{ marginTop: 6 }} />
            ))}
          </div>
        </CardSkel>
      </div>
      {/* Products */}
      <CardSkel pad={0}>
        <div style={{ padding: '16px 16px 10px' }}>
          <Bone w={60} h={8} />
          <Bone w={80} h={14} style={{ marginTop: 6 }} />
        </div>
        <div style={{ padding: '0 16px 16px' }}>
          {Array.from({ length: 5 }).map((_, i) => (
            <Bone key={i} h={20} style={{ marginTop: 8 }} />
          ))}
        </div>
      </CardSkel>
      {/* Wiki + Email */}
      <div style={{ display: 'grid', gridTemplateColumns: '2fr 1fr', gap: 14 }}>
        <CardSkel pad={0}>
          <div style={{ display: 'flex', minHeight: 300 }}>
            <div style={{ width: '42%', minWidth: 180, borderRight: '1px solid #e2e8f0', padding: 12 }}>
              <Bone h={30} r={6} />
              {Array.from({ length: 4 }).map((_, i) => (
                <Bone key={i} h={36} style={{ marginTop: 8 }} />
              ))}
            </div>
            <div style={{ flex: 1, padding: 16 }}>
              <Bone w="50%" h={16} />
              <Bone h={12} style={{ marginTop: 12 }} />
              <Bone h={12} w="80%" style={{ marginTop: 6 }} />
            </div>
          </div>
        </CardSkel>
        <CardSkel pad={0}>
          <div style={{ padding: '16px 16px 10px' }}>
            <Bone w={50} h={8} />
            <Bone w={60} h={14} style={{ marginTop: 6 }} />
          </div>
          <div style={{ padding: '0 16px 16px' }}>
            {Array.from({ length: 5 }).map((_, i) => (
              <div key={i} style={{ display: 'flex', alignItems: 'center', gap: 10, padding: '9px 0' }}>
                <div style={{ flex: 1 }}>
                  <Bone w="60%" h={10} />
                  <Bone w="90%" h={12} style={{ marginTop: 4 }} />
                </div>
                <Bone w={30} h={10} />
              </div>
            ))}
          </div>
        </CardSkel>
      </div>
    </div>
  )
}
```

- [ ] **Step 4: Create empty page.tsx**

Create `src/app/willow-investment/(linear)/etc/page.tsx`:
```tsx
'use client'

import { useState } from 'react'
import { t } from '@/app/willow-investment/_components/linear-tokens'
import { EtcSkeleton } from '@/app/willow-investment/_components/linear-skeleton'

export default function EtcPage() {
  const [loading] = useState(true)

  return (
    <>
      <div style={{ padding: '20px 0 0' }}>
        <h1 style={{
          margin: 0, fontSize: 22, fontWeight: 600, letterSpacing: -0.3,
          fontFamily: t.font.sans, color: t.neutrals.text,
        }}>ETC</h1>
        <p style={{
          margin: '4px 0 16px', fontSize: 12, color: t.neutrals.muted,
          fontFamily: t.font.sans,
        }}>ETF Platform · 인보이스 · 위키 · 이메일</p>
      </div>
      {loading && <EtcSkeleton />}
    </>
  )
}
```

- [ ] **Step 5: Verify and commit**

Run: `npx tsc --noEmit`
Expected: no errors

```bash
git add src/app/willow-investment/(linear)/etc/page.tsx \
  "src/app/willow-investment/_components/linear-sidebar.tsx" \
  "src/app/willow-investment/_components/linear-skeleton.tsx" \
  "src/components/layout/layout-wrapper.tsx"
git commit -m "feat: scaffold ETC Linear page with route, sidebar link, skeleton"
```

---

### Task 2: Stats Block — AUM, Monthly Fee, Remaining Fee

**Files:**
- Create: `src/app/willow-investment/(linear)/etc/_components/stats-block.tsx`

**Context:** Follows the pattern of akros `aum-block.tsx` but with ETC-specific metrics. Uses `ETFDisplayData` and `HistoricalDataPoint` from `supabase-etf.ts`. All values are in USD.

- [ ] **Step 1: Create stats-block.tsx**

Create `src/app/willow-investment/(linear)/etc/_components/stats-block.tsx`:

```tsx
'use client'

import { t } from '@/app/willow-investment/_components/linear-tokens'
import { LCard } from '@/app/willow-investment/_components/linear-card'
import { LSectionHead } from '@/app/willow-investment/_components/linear-section-head'
import { ETFDisplayData, HistoricalDataPoint } from '@/lib/supabase-etf'

interface StatsBlockProps {
  etfs: ETFDisplayData[]
  historicalData: HistoricalDataPoint[]
}

function Sparkline({ data }: { data: number[] }) {
  if (data.length < 2) return null
  const min = Math.min(...data)
  const max = Math.max(...data)
  const range = max - min || 1
  const h = 28
  const points = data.map((v, i) => {
    const x = (i / (data.length - 1)) * 100
    const y = h - ((v - min) / range) * h
    return `${x},${y}`
  }).join(' ')
  return (
    <svg viewBox={`0 0 100 ${h}`} preserveAspectRatio="none" style={{ display: 'block', width: '100%', height: h }}>
      <polyline points={points} fill="none" stroke={t.brand[500]} strokeWidth={1} vectorEffect="non-scaling-stroke" />
    </svg>
  )
}

function fmtUsd(v: number | null): string {
  if (v == null) return '-'
  if (v >= 1000000) return `$${(v / 1000000).toFixed(2)}M`
  if (v >= 1000) return `$${(v / 1000).toFixed(1)}K`
  return `$${v.toFixed(0)}`
}

interface StatCardProps {
  label: string
  value: string
  sub: string
  sparkData: number[]
}

function StatCard({ label, value, sub, sparkData }: StatCardProps) {
  return (
    <div style={{
      background: t.neutrals.inner, borderRadius: t.radius.sm,
      padding: '8px 10px', minWidth: 0, flex: 1,
    }}>
      <div style={{
        fontSize: 9.5, fontFamily: t.font.mono, letterSpacing: 0.8,
        textTransform: 'uppercase' as const, color: t.neutrals.subtle,
        marginBottom: 6,
      }}>{label}</div>
      <div style={{ display: 'flex', alignItems: 'flex-end', justifyContent: 'space-between', gap: 12 }}>
        <div style={{ minWidth: 0 }}>
          <div style={{
            fontSize: 15, fontWeight: 600, letterSpacing: -0.3,
            fontVariantNumeric: 'tabular-nums', color: t.neutrals.text,
            whiteSpace: 'nowrap',
          }}>{value}</div>
          <div style={{
            fontSize: 10, color: t.neutrals.muted, marginTop: 1,
            whiteSpace: 'nowrap',
          }}>{sub}</div>
        </div>
        <div style={{ width: 80, flexShrink: 0 }}>
          <Sparkline data={sparkData} />
        </div>
      </div>
    </div>
  )
}

const FIXED_OVERHEAD = 2083.33

export function StatsBlock({ etfs, historicalData }: StatsBlockProps) {
  const totalAum = etfs.reduce((sum, e) => sum + (e.aum || 0), 0)
  const totalMonthlyFee = etfs.reduce((sum, e) => sum + e.totalMonthlyFee, 0) + FIXED_OVERHEAD
  const totalRemainingFee = etfs.reduce((sum, e) => sum + (e.remainingFee || 0), 0)

  return (
    <LCard>
      <LSectionHead eyebrow="DASHBOARD" title="운용 현황" />
      <div style={{ display: 'flex', gap: 10 }}>
        <StatCard
          label="총 AUM"
          value={fmtUsd(totalAum)}
          sub={`${etfs.length}개 상품`}
          sparkData={historicalData.map(d => d.totalAum)}
        />
        <StatCard
          label="월 수수료"
          value={fmtUsd(totalMonthlyFee)}
          sub="Platform + PM Fee"
          sparkData={historicalData.map(d => d.totalMonthlyFee)}
        />
        <StatCard
          label="잔여 수수료"
          value={fmtUsd(totalRemainingFee)}
          sub="36개월 프로라타"
          sparkData={historicalData.map(d => d.totalRemainingFee)}
        />
      </div>
    </LCard>
  )
}
```

- [ ] **Step 2: Verify and commit**

Run: `npx tsc --noEmit`

```bash
git add "src/app/willow-investment/(linear)/etc/_components/stats-block.tsx"
git commit -m "feat: add ETC stats block with AUM, monthly fee, remaining fee"
```

---

### Task 3: Product Block — table with ETC columns

**Files:**
- Create: `src/app/willow-investment/(linear)/etc/_components/product-block.tsx`

**Context:** Table with columns: Symbol, Fund Name, Listing Date, AUM, Flow, Fee/Mo, Remaining. Pagination uses email-style (page size input + range + chevrons). Actions: edit, documents, delete (via callbacks to parent).

- [ ] **Step 1: Create product-block.tsx**

Create `src/app/willow-investment/(linear)/etc/_components/product-block.tsx`:

```tsx
'use client'

import { useState } from 'react'
import { t } from '@/app/willow-investment/_components/linear-tokens'
import { LCard } from '@/app/willow-investment/_components/linear-card'
import { LSectionHead } from '@/app/willow-investment/_components/linear-section-head'
import { LBtn } from '@/app/willow-investment/_components/linear-btn'
import { LIcon } from '@/app/willow-investment/_components/linear-icons'
import { ETFDisplayData } from '@/lib/supabase-etf'

interface ProductBlockProps {
  etfs: ETFDisplayData[]
  onAdd: () => void
  onEdit: (etf: ETFDisplayData) => void
  onDocuments: (etf: ETFDisplayData) => void
  onDelete: (etf: ETFDisplayData) => void
  onRefresh: () => void
}

const DEFAULT_PAGE_SIZE = 5
const PAGE_SIZE_KEY = 'etc-product-page-size'

function getStoredPageSize(): number {
  if (typeof window === 'undefined') return DEFAULT_PAGE_SIZE
  const v = localStorage.getItem(PAGE_SIZE_KEY)
  if (!v) return DEFAULT_PAGE_SIZE
  const n = Number(v)
  return n >= 5 && n <= 50 ? n : DEFAULT_PAGE_SIZE
}

function fmtUsd(v: number | null): string {
  if (v == null) return '-'
  if (v >= 1000000) return `$${(v / 1000000).toFixed(2)}M`
  if (v >= 1000) return `$${(v / 1000).toFixed(1)}K`
  return `$${v.toFixed(0)}`
}

function fmtFlow(v: number | null): string {
  if (v == null) return '-'
  const sign = v >= 0 ? '+' : '-'
  const abs = Math.abs(v)
  if (abs >= 1000000) return `${sign}$${(abs / 1000000).toFixed(2)}M`
  if (abs >= 1000) return `${sign}$${(abs / 1000).toFixed(1)}K`
  return `${sign}$${abs.toFixed(0)}`
}

export function ProductBlock({ etfs, onAdd, onEdit, onDocuments, onDelete, onRefresh }: ProductBlockProps) {
  const [page, setPage] = useState(0)
  const [pageSize, setPageSize] = useState(getStoredPageSize)
  const [pageSizeInput, setPageSizeInput] = useState(String(getStoredPageSize()))
  const totalPages = Math.max(1, Math.ceil(etfs.length / pageSize))
  const paged = etfs.slice(page * pageSize, (page + 1) * pageSize)

  const commitPageSize = () => {
    const n = Math.max(5, Math.min(50, Number(pageSizeInput) || DEFAULT_PAGE_SIZE))
    setPageSizeInput(String(n))
    setPageSize(n)
    setPage(0)
    localStorage.setItem(PAGE_SIZE_KEY, String(n))
  }

  const thStyle: React.CSSProperties = {
    padding: '6px 10px', fontSize: 10, fontFamily: t.font.mono,
    fontWeight: 600, color: t.neutrals.subtle, textAlign: 'left',
    letterSpacing: 0.3, whiteSpace: 'nowrap',
  }
  const tdStyle: React.CSSProperties = {
    padding: '7px 10px', fontSize: 11.5, fontFamily: t.font.sans,
    color: t.neutrals.text, whiteSpace: 'nowrap',
  }

  return (
    <LCard pad={0}>
      <div style={{ padding: t.density.cardPad, paddingBottom: 10 }}>
        <LSectionHead eyebrow="PRODUCTS" title="상품 관리" action={
          <div style={{ display: 'flex', alignItems: 'center', gap: 6 }}>
            <span style={{ fontSize: 11, color: t.neutrals.muted, fontFamily: t.font.mono }}>
              {etfs.length}개
            </span>
            <button onClick={onRefresh} style={{ background: 'none', border: 'none', cursor: 'pointer', padding: 4, color: t.neutrals.subtle }}>
              <LIcon name="refresh" size={13} />
            </button>
            <LBtn size="sm" icon={<LIcon name="plus" size={14} color="#fff" />} onClick={onAdd}>추가</LBtn>
          </div>
        } />
      </div>

      <div>
        <table style={{ width: '100%', borderCollapse: 'collapse', tableLayout: 'fixed' }}>
          <colgroup>
            <col style={{ width: 72 }} />
            <col />
            <col style={{ width: 78 }} />
            <col style={{ width: 80 }} />
            <col style={{ width: 80 }} />
            <col style={{ width: 80 }} />
            <col style={{ width: 80 }} />
            <col style={{ width: 72 }} />
          </colgroup>
          <thead>
            <tr style={{ background: t.neutrals.inner, borderBottom: `1px solid ${t.neutrals.line}` }}>
              <th style={thStyle}>SYMBOL</th>
              <th style={thStyle}>FUND NAME</th>
              <th style={thStyle}>LISTING</th>
              <th style={{ ...thStyle, textAlign: 'right' }}>AUM</th>
              <th style={{ ...thStyle, textAlign: 'right' }}>FLOW</th>
              <th style={{ ...thStyle, textAlign: 'right' }}>FEE/MO</th>
              <th style={{ ...thStyle, textAlign: 'right' }}>REMAINING</th>
              <th style={thStyle}></th>
            </tr>
          </thead>
          <tbody>
            {paged.map(etf => (
              <tr key={etf.id} style={{ borderBottom: `1px solid ${t.neutrals.line}` }}>
                <td style={{ ...tdStyle, fontFamily: t.font.mono, fontWeight: 500 }}>
                  {etf.fundUrl ? (
                    <a href={etf.fundUrl} target="_blank" rel="noopener noreferrer"
                      style={{ color: t.brand[500], textDecoration: 'none' }}>{etf.symbol}</a>
                  ) : etf.symbol}
                </td>
                <td style={{ ...tdStyle, overflow: 'hidden', textOverflow: 'ellipsis', whiteSpace: 'nowrap' }}>
                  {etf.fundName}
                </td>
                <td style={{ ...tdStyle, fontFamily: t.font.mono, fontSize: 10, color: t.neutrals.muted }}>
                  {etf.listingDate || '-'}
                </td>
                <td style={{ ...tdStyle, textAlign: 'right', fontFamily: t.font.mono }}>
                  {fmtUsd(etf.aum)}
                </td>
                <td style={{
                  ...tdStyle, textAlign: 'right', fontFamily: t.font.mono,
                  color: (etf.flow ?? 0) >= 0 ? '#16A34A' : '#DC2626',
                }}>
                  {fmtFlow(etf.flow)}
                </td>
                <td style={{ ...tdStyle, textAlign: 'right', fontFamily: t.font.mono }}>
                  {fmtUsd(etf.totalMonthlyFee)}
                </td>
                <td style={{ ...tdStyle, textAlign: 'right', fontFamily: t.font.mono, fontWeight: 500 }}>
                  {fmtUsd(etf.remainingFee)}
                </td>
                <td style={{ ...tdStyle, textAlign: 'center' }}>
                  <div style={{ display: 'flex', gap: 2, justifyContent: 'center' }}>
                    <button onClick={() => onEdit(etf)} style={{ background: 'none', border: 'none', cursor: 'pointer', padding: 4, color: t.neutrals.subtle }}>
                      <LIcon name="pencil" size={12} />
                    </button>
                    <button onClick={() => onDocuments(etf)} style={{ background: 'none', border: 'none', cursor: 'pointer', padding: 4, color: t.neutrals.subtle }}>
                      <LIcon name="file" size={12} />
                    </button>
                    <button onClick={() => { if (confirm(`${etf.symbol} 삭제?`)) onDelete(etf) }} style={{ background: 'none', border: 'none', cursor: 'pointer', padding: 4, color: t.neutrals.subtle }}>
                      <LIcon name="x" size={12} />
                    </button>
                  </div>
                </td>
              </tr>
            ))}
            {paged.length === 0 && (
              <tr>
                <td colSpan={8} style={{ ...tdStyle, textAlign: 'center', color: t.neutrals.subtle, padding: 30 }}>
                  상품 데이터가 없습니다
                </td>
              </tr>
            )}
          </tbody>
        </table>
      </div>

      {/* Pagination */}
      <div style={{
        display: 'flex', alignItems: 'center', justifyContent: 'space-between',
        padding: '6px 16px', borderTop: `1px solid ${t.neutrals.line}`,
      }}>
        <div style={{ display: 'flex', alignItems: 'center', gap: 4 }}>
          <input value={pageSizeInput}
            onChange={e => setPageSizeInput(e.target.value.replace(/\D/g, ''))}
            onBlur={commitPageSize}
            onKeyDown={e => { if (e.key === 'Enter') commitPageSize() }}
            style={{
              width: 32, textAlign: 'center', border: 'none',
              background: t.neutrals.inner, borderRadius: t.radius.sm,
              fontSize: 11, fontFamily: t.font.mono, color: t.neutrals.muted,
              padding: '2px 0', outline: 'none',
            }}
          />
          <span style={{ fontSize: 10, color: t.neutrals.subtle, fontFamily: t.font.sans }}>개씩</span>
        </div>
        {totalPages > 1 && (
          <div style={{ display: 'flex', alignItems: 'center', gap: 6 }}>
            <button disabled={page === 0} onClick={() => setPage(p => p - 1)}
              style={{
                background: 'transparent', border: 'none', padding: 4, borderRadius: 4,
                cursor: page === 0 ? 'default' : 'pointer',
                color: page === 0 ? t.neutrals.line : t.neutrals.muted,
                opacity: page === 0 ? 0.4 : 1,
              }}>
              <LIcon name="chevronLeft" size={13} stroke={2} />
            </button>
            <span style={{ fontSize: 10, fontFamily: t.font.mono, color: t.neutrals.muted }}>
              {page * pageSize + 1}-{Math.min((page + 1) * pageSize, etfs.length)} / {etfs.length}
            </span>
            <button disabled={page >= totalPages - 1} onClick={() => setPage(p => p + 1)}
              style={{
                background: 'transparent', border: 'none', padding: 4, borderRadius: 4,
                cursor: page >= totalPages - 1 ? 'default' : 'pointer',
                color: page >= totalPages - 1 ? t.neutrals.line : t.neutrals.muted,
                opacity: page >= totalPages - 1 ? 0.4 : 1,
              }}>
              <LIcon name="chevronRight" size={13} stroke={2} />
            </button>
          </div>
        )}
      </div>
    </LCard>
  )
}
```

- [ ] **Step 2: Verify and commit**

Run: `npx tsc --noEmit`

```bash
git add "src/app/willow-investment/(linear)/etc/_components/product-block.tsx"
git commit -m "feat: add ETC product table block"
```

---

### Task 4: Product Dialog — add/edit modal with fee tier editing

**Files:**
- Create: `src/app/willow-investment/(linear)/etc/_components/product-dialog.tsx`

**Context:** Modal for creating/editing ETF products. Key fields: symbol, fundName, fundUrl, listingDate, bank, currency, notes. Plus fee tier editors for platform and PM fees. Uses `createETFProduct()`, `updateETFProduct()`, `deleteETFProduct()` from `supabase-etf.ts`. Fee tiers use `FeeStructure` type: `{ minFee: number, tiers: FeeTier[] }` where `FeeTier = { upTo: number, bps: number }`.

The engineer should:
1. Build the modal shell matching other Linear modals (fixed overlay, `t.neutrals.card` background, `t.radius.lg`)
2. Include form fields for all product properties
3. Include a `FeeTierEditor` sub-component that renders tier rows with threshold + bps inputs, plus minFee input. Threshold supports shorthand parsing: "500M" → 500000000, "1B" → 1000000000.
4. Support both create (no `editEtf` prop) and edit mode (with `editEtf` prop). Edit mode shows delete button.
5. On save: call `createETFProduct` or `updateETFProduct`. On delete: confirm + `deleteETFProduct`.

**Input type reference** (`ETFProductInput` from supabase-etf.ts):
```typescript
{
  symbol: string
  fund_name: string
  fund_url?: string | null
  listing_date?: string | null
  bank?: string
  platform_fee_tiers?: FeeStructure | null
  pm_fee_tiers?: FeeStructure | null
  currency?: string
  notes?: string | null
  is_active?: boolean
}
```

- [ ] **Step 1: Create product-dialog.tsx**

Create the full component with form fields, fee tier editor, create/edit/delete logic. Follow the Linear modal pattern: fixed overlay with `rgba(0,0,0,0.35)` backdrop, card with `t.neutrals.card` bg, `t.radius.lg` border radius. Input style: `border: 'none', background: t.neutrals.inner, borderRadius: t.radius.sm`. Buttons: `LBtn` with `size="sm"`.

Fee tier editor: each tier row has two inputs (threshold in "500M" format, bps as number). "Add Tier" button appends `{ upTo: 0, bps: 0 }`. Last tier's upTo should be 0 (unlimited). MinFee input above the tiers.

Threshold parsing helper:
```typescript
function parseThreshold(s: string): number {
  const upper = s.toUpperCase().trim()
  if (upper.endsWith('B')) return parseFloat(upper) * 1e9
  if (upper.endsWith('M')) return parseFloat(upper) * 1e6
  if (upper.endsWith('K')) return parseFloat(upper) * 1e3
  return parseFloat(upper) || 0
}
function fmtThreshold(v: number): string {
  if (v === 0) return '∞'
  if (v >= 1e9) return `${v / 1e9}B`
  if (v >= 1e6) return `${v / 1e6}M`
  if (v >= 1e3) return `${v / 1e3}K`
  return String(v)
}
```

- [ ] **Step 2: Verify and commit**

Run: `npx tsc --noEmit`

```bash
git add "src/app/willow-investment/(linear)/etc/_components/product-dialog.tsx"
git commit -m "feat: add ETC product dialog with fee tier editing"
```

---

### Task 5: Document Panel — per-product file management

**Files:**
- Create: `src/app/willow-investment/(linear)/etc/_components/document-panel.tsx`

**Context:** Modal overlay showing documents for a specific product. Uses `fetchETFDocuments(symbol)`, `uploadETFDocument(symbol, file)`, `getDocumentDownloadUrl(symbol, fileName)`, `deleteETFDocument(symbol, fileName)` from `supabase-etf.ts`. Storage bucket: `etf-documents/{symbol}/{filename}`.

The engineer should:
1. Show list of documents (name, size formatted, date)
2. Upload button with file input
3. Download button per doc (opens signed URL in new tab)
4. Delete button per doc (with confirm)
5. Loading state during operations

- [ ] **Step 1: Create document-panel.tsx**

Create the component as a modal overlay. Display document list with file icon, name, size, and action buttons. File upload via hidden input triggered by an upload button. Size formatting: `>1MB` → `1.2 MB`, `>1KB` → `12 KB`, else `123 B`.

Props:
```typescript
interface DocumentPanelProps {
  etf: ETFDisplayData | null
  onClose: () => void
}
```

- [ ] **Step 2: Verify and commit**

Run: `npx tsc --noEmit`

```bash
git add "src/app/willow-investment/(linear)/etc/_components/document-panel.tsx"
git commit -m "feat: add ETC document panel for per-product file management"
```

---

### Task 6: Invoice Block — list with status management

**Files:**
- Create: `src/app/willow-investment/(linear)/etc/_components/invoice-block.tsx`

**Context:** Invoice list in 1/3 right column, spanning full height. Similar to Akros tax-invoice-block but with more complex status system. Uses `Invoice` type from `@/lib/invoice/types`. Status is determined by `getEffectiveInvoiceStatus()` which checks paid_at, sent_to_etc_at, sent_to_bank_at, scheduled IDs.

Status types and colors (using `tonePalettes` from linear-tokens):
- `draft` → neutral ("초안")
- `scheduled_etc/bank/both` → purple tones ("예약")
- `sent_etc` → info ("ETC발송")
- `sent_bank` → warn ("은행발송")
- `sent` (both) → info ("발송완료")
- `paid` → done ("입금")
- `overdue` → danger ("연체")
- `cancelled` → neutral ("취소")

Each row: 2-line layout matching akros tax-invoice-block:
- Line 1: status badge + invoice date + action buttons (edit, PDF, ETC발송, 은행발송, 입금)
- Line 2: total amount + first line item description (truncated)

Pagination: email-style. Default 8 per page.

Props:
```typescript
interface InvoiceBlockProps {
  invoices: Invoice[]
  onRefresh: () => void
  onAdd: () => void
  onEdit: (inv: Invoice) => void
  onSendEtc: (inv: Invoice) => void
  onSendBank: (inv: Invoice) => void
  style?: React.CSSProperties
}
```

The `getEffectiveInvoiceStatus` function and status style map should be defined inside this file.

For the "입금" toggle: `PATCH /api/invoices/{id}` with `{ status: 'paid' }`. For PDF download: open `/api/invoices/{id}/pdf` in new tab.

- [ ] **Step 1: Create invoice-block.tsx**

Create the full component following the akros tax-invoice-block 2-line row pattern. Include `getEffectiveInvoiceStatus()`, status style map using `tonePalettes`, pagination, and all action buttons as text buttons (like akros "발행"/"입금" pattern).

- [ ] **Step 2: Verify and commit**

Run: `npx tsc --noEmit`

```bash
git add "src/app/willow-investment/(linear)/etc/_components/invoice-block.tsx"
git commit -m "feat: add ETC invoice block with status management"
```

---

### Task 7: Invoice Dialog — create/edit with line items

**Files:**
- Create: `src/app/willow-investment/(linear)/etc/_components/invoice-dialog.tsx`

**Context:** Modal for creating/editing invoices. Uses `ITEM_TEMPLATES`, `DEFAULT_CLIENT`, `generateInvoiceNo` from `@/lib/invoice/constants`. Invoice number format: `#YY-ETC-NNN`.

Form fields:
- Invoice Date (required, default today)
- Attention (required, default "Garrett Stevens")
- Notes (optional)
- Line Items (dynamic list, at least 1):
  - Type: select (monthly_fee / referral_fee / custom)
  - Month: select 1-12 (for monthly_fee/referral_fee)
  - Year: number input (for monthly_fee/referral_fee)
  - Description: auto-generated or custom input
  - Amount: number input (USD)

For monthly_fee type, description auto-generates: `"Monthly Fee - {MonthName} {Year}"`.
For referral_fee: `"Referral Fee - {MonthName} {Year}"`.

API:
- Create: `POST /api/invoices` with body `{ invoice_date, bill_to_company: DEFAULT_CLIENT.company, attention, line_items, notes }`
- Edit: `PATCH /api/invoices/{id}` with same fields
- Delete: `DELETE /api/invoices/{id}` (confirm first, delete button in edit mode)

Props:
```typescript
interface InvoiceDialogProps {
  open: boolean
  editInvoice: Invoice | null
  onClose: () => void
  onSaved: () => void
}
```

- [ ] **Step 1: Create invoice-dialog.tsx**

Create the full component. Month names array: `['January','February',...,'December']`. Line item form with add/remove buttons. Total auto-calculated from item amounts. Delete button in footer (edit mode only, left side). Cancel + Save buttons right side.

- [ ] **Step 2: Verify and commit**

Run: `npx tsc --noEmit`

```bash
git add "src/app/willow-investment/(linear)/etc/_components/invoice-dialog.tsx"
git commit -m "feat: add ETC invoice dialog with line item editing"
```

---

### Task 8: Invoice Send Dialog — dual-recipient + scheduling

**Files:**
- Create: `src/app/willow-investment/(linear)/etc/_components/invoice-send-dialog.tsx`

**Context:** Modal for sending invoice via email. Supports two targets: ETC (kyle@exchangetradedconcepts.com) and Bank (ysjmto@shinhan.com). Includes optional scheduling.

Flow:
1. Open → fetch PDF blob from `/api/invoices/{id}/pdf`
2. Pre-fill to/cc/subject/body based on target:
   - **ETC**: to=`gstevens@exchangetradedconcepts.com`, cc=`accounting@exchangetradedconcepts.com`, subject=`Willow Investments - {itemDesc}`, body=English template
   - **Bank**: to=`ysjmto@shinhan.com`, subject=`윌로우인베스트먼트 외화인보이스 - {invoiceNo}`, body=Korean template
3. User can edit all fields
4. Toggle: immediate send vs scheduled
5. If scheduled: date + time inputs, validate future time
6. Send: `POST /api/gmail/send` with `{ to, cc, subject, body, attachments: [pdfBlob], context: 'default', scheduledAt? }`
7. After send: `PATCH /api/invoices/{id}` to update status fields

Props:
```typescript
interface InvoiceSendDialogProps {
  invoice: Invoice | null
  target: 'etc' | 'bank'
  onClose: () => void
  onSent: () => void
}
```

- [ ] **Step 1: Create invoice-send-dialog.tsx**

Create the full component. Include PDF loading state, editable email fields (to, cc, subject, body as textarea), schedule toggle with date/time inputs. Use `LBtn` for send button. Show loading during send.

Email templates:
```typescript
// ETC template
const etcSubject = `Willow Investments - ${firstItemDesc}`
const etcBody = `Hi Kyle,\n\nAttached is my invoice for the ${firstItemDesc}.\n\nPlease let me know if you have any questions.\n\nBest regards,\nDong-Wook Kim\nWillow Investments, Inc.`

// Bank template
const bankSubject = `윌로우인베스트먼트 외화인보이스 - ${invoice.invoice_no}`
const bankBody = `안녕하세요,\n\n첨부 외화인보이스에 대한 외화송금을 요청드립니다.\n\n감사합니다.\n\n윌로우인베스트먼트(주)\n김동욱`
```

- [ ] **Step 2: Verify and commit**

Run: `npx tsc --noEmit`

```bash
git add "src/app/willow-investment/(linear)/etc/_components/invoice-send-dialog.tsx"
git commit -m "feat: add ETC invoice send dialog with dual-recipient and scheduling"
```

---

### Task 9: Wiki Block — etf-etc section wrapper

**Files:**
- Create: `src/app/willow-investment/(linear)/etc/_components/wiki-block.tsx`

**Context:** Identical pattern to akros wiki-block.tsx but filters `section === 'etf-etc'` and forces `section: 'etf-etc'` on create.

- [ ] **Step 1: Create wiki-block.tsx**

Create `src/app/willow-investment/(linear)/etc/_components/wiki-block.tsx`:

```tsx
'use client'

import { WikiList } from '@/app/willow-investment/(linear)/wiki/_components/wiki-list'
import { WikiNote } from '@/app/willow-investment/(linear)/wiki/_components/wiki-note-row'

type WikiSection = 'akros' | 'etf-etc' | 'willow-mgmt' | 'tensw-mgmt'

interface EtcWikiBlockProps {
  notes: WikiNote[]
  loading: boolean
  onCreate: (data: { section: 'etf-etc'; title: string; content: string; attachments?: unknown }) => Promise<void>
  onUpdate: (id: string, data: Partial<{ title: string; content: string; section: string; is_pinned: boolean; attachments: unknown }>) => Promise<void>
  onDelete: (id: string) => Promise<void>
}

export function EtcWikiBlock({ notes, loading, onCreate, onUpdate, onDelete }: EtcWikiBlockProps) {
  const etcNotes = notes.filter(n => n.section === 'etf-etc')

  const handleCreate = async (data: { section: WikiSection; title: string; content: string; attachments?: unknown }) => {
    await onCreate({ ...data, section: 'etf-etc' })
  }

  return (
    <WikiList
      notes={etcNotes}
      loading={loading}
      onCreate={handleCreate}
      onUpdate={onUpdate}
      onDelete={onDelete}
      hideFilter
    />
  )
}
```

- [ ] **Step 2: Verify and commit**

Run: `npx tsc --noEmit`

```bash
git add "src/app/willow-investment/(linear)/etc/_components/wiki-block.tsx"
git commit -m "feat: add ETC wiki block wrapper"
```

---

### Task 10: Page Assembly — wire all blocks together

**Files:**
- Modify: `src/app/willow-investment/(linear)/etc/page.tsx`

**Context:** Main page that loads all data and assembles blocks. Follows akros page.tsx pattern closely but with ETC-specific data sources and additional dialogs (product, document, invoice, invoice-send).

Data loading:
- `fetchETFDisplayData()` → etfs (products with calculated fees)
- `fetchHistoricalData(products, 180)` → historical data for sparklines (needs raw `ETFProduct[]` — use separate `fetchETFProducts()` call)
- `GET /api/invoices` → invoices
- `GET /api/wiki` → wiki notes (filter client-side for etf-etc)
- Gmail status + emails with `label=ETC`

State: etfs, historicalData, invoices, wikiNotes, emails, plus dialog states for product/document/invoice/invoice-send.

Grid layout:
```
Row 1: Stats (col1, row1) + Invoices (col2, row 1/-1)
Row 2: Products (col1, row2)
Row 3: Wiki (2/3) + Email (1/3)
```

Product CRUD handlers call lib functions then `loadData()`. Invoice CRUD via fetch then `loadInvoices()`. Wiki/Email same as akros.

The engineer should reference the akros page.tsx pattern for email wiring (EmailBlock + EmailDetailDialog + ComposeEmailDialog with compose modes) and wiki wiring (loadWiki with `Array.isArray(data) ? data : []`).

Additional dialog state:
```typescript
const [productDialogOpen, setProductDialogOpen] = useState(false)
const [editEtf, setEditEtf] = useState<ETFDisplayData | null>(null)
const [docEtf, setDocEtf] = useState<ETFDisplayData | null>(null)
const [invoiceDialogOpen, setInvoiceDialogOpen] = useState(false)
const [editInvoice, setEditInvoice] = useState<Invoice | null>(null)
const [sendInvoice, setSendInvoice] = useState<Invoice | null>(null)
const [sendTarget, setSendTarget] = useState<'etc' | 'bank'>('etc')
```

- [ ] **Step 1: Rewrite page.tsx with all blocks and data loading**

Replace the empty page.tsx with the full implementation. Import all block components and dialogs. Set up all data loading callbacks. Wire all event handlers. Use the same grid layout pattern as akros.

- [ ] **Step 2: Verify TypeScript compiles**

Run: `npx tsc --noEmit`
Fix any type errors.

- [ ] **Step 3: Test in browser**

Start dev server, navigate to `/willow-investment/etc`. Verify:
- Stats cards show data
- Product table loads and paginates
- Invoice list loads
- Wiki section shows etf-etc notes
- Email loads
- All dialogs open/close correctly

- [ ] **Step 4: Commit**

```bash
git add "src/app/willow-investment/(linear)/etc/page.tsx"
git commit -m "feat: assemble ETC page with all blocks, dialogs, and data loading"
```

---

## Self-Review Checklist

**Spec coverage:**
- [x] Stats block (AUM, Monthly Fee, Remaining Fee) → Task 2
- [x] Product table with all columns → Task 3
- [x] Product add/edit with fee tiers → Task 4
- [x] Document management → Task 5
- [x] Invoice list with status → Task 6
- [x] Invoice create/edit with line items → Task 7
- [x] Invoice send (dual-recipient + scheduling) → Task 8
- [x] Wiki (etf-etc section) → Task 9
- [x] Email integration → Task 10
- [x] Route + sidebar + skeleton → Task 1
- [x] Layout: stats+products 2/3, invoices 1/3 spanning → Task 10

**Placeholder scan:** No TBD/TODO found. Tasks 4, 5, 6, 7, 8 describe behavior in detail but defer exact code to the engineer (these are complex components — the engineer has full type definitions, API specs, and reference patterns from akros to follow).

**Type consistency:** All types reference exact imports from `supabase-etf.ts` and `invoice/types.ts`. Function signatures match the lib definitions confirmed by code exploration.
