# 청킹 번역(Chunk Translation) 스킬 가이드

영어 학습 자료를 **영어 어순 그대로** 청크(의미 구절) 단위로 끊어 한국어와 1:1로 병렬 정렬한 CSV를 만드는 방법 모음입니다. 결과물은 엑셀/구글시트에서 바로 보거나 플래시카드(Anki 등)로 임포트할 수 있습니다.

핵심 아이디어 하나: **한국어를 자연스러운 우리말 어순으로 재배열하지 않는다.** 영어가 나오는 순서 그대로, 그 위치의 청크만 직역한다. 이래야 "영어 어순대로 이해하는" 직독직해 훈련이 된다.

---

## 공통 규칙 (모든 청킹 번역에 적용)

### 번역 원칙
- **청크 N의 한국어 = 청크 N의 영어 직역** (위치 1:1 매칭 필수)
- 한국어 청크 순서 = 영어 청크 순서 (재배열 금지)
- 각 청크 자체는 자연스러운 한국어 (조사·어미 조정은 OK)
- 고유명사·숫자·단위는 원문 그대로 유지 (예: `Ideal-X`, `150-acre`)
- 전문 용어는 원어 병기 가능 (예: `컨테이너 크레인(stacking crane)`)

### 좋은 예 vs 나쁜 예

| 영어 청크 | ❌ 나쁜 번역 (우리말 어순 재배열) | ✅ 좋은 번역 (영어 위치 1:1) |
|-----------|-------------------------------|-------------------------------|
| I tethered Frightful | 내가 호두나무로 가서 | 나는 프라이트풀을 |
| in the hickory tree | 주머니를 채우는 동안 | 히코리 나무에 묶어 두고 |
| while I went to the walnut tree | 프라이트풀을 히코리 나무에 | 호두나무로 가서 |
| and filled pouches. | 묶어 두었다. | 주머니를 채웠다. |

> 왼쪽은 우리말 어순으로 뒤섞어서 영어 청크 위치와 어긋난다.
> 오른쪽은 각 청크가 영어와 같은 자리에서 같은 의미를 전달한다.

### CSV 파일 규칙 (공통)
- **인코딩**: UTF-8 with BOM (맨 앞에 `﻿` 문자) — 엑셀에서 한글이 깨지지 않게
- **따옴표**: 모든 셀을 큰따옴표(`"`)로 감쌈 (줄바꿈·쉼표 포함 셀 보호)
- **셀 안 줄바꿈**: 반드시 **실제 줄바꿈 문자**(0x0A)를 넣는다. `\n` 같은 리터럴 문자열 금지 → 엑셀에서 Alt+Enter 줄바꿈처럼 보여야 함
- 셀 안에 큰따옴표가 필요하면 `""`로 두 번 써서 이스케이프 (예: `"그는 ""안녕""이라고 말했다."`)

---

## 스킬 1 — 지문 청킹 번역 (문단/책 텍스트)

영어 문단·책 발췌문을 청크별 병렬 번역 CSV로 만든다.

### 언제 쓰나
- 영어 지문을 주며 "청크 번역해줘", "청크별 한글 번역", "병렬 번역 CSV로" 요청할 때

### 출력 형식 (2열)
| 열 | 내용 |
|----|------|
| A: Question | 한국어 (청크별 줄바꿈) |
| B: Answer | 영어 원문 (청크별 줄바꿈) |

플래시카드로 쓸 때: Question(한국어)=앞면, Answer(영어)=뒷면.

### 행 구성 — 두 가지 방식

**방식 A (기본) — 한 문장 = 한 행**
문장 하나의 모든 청크를 한 셀 안에 줄바꿈으로 묶는다. Question 셀의 줄바꿈 수 = Answer 셀의 줄바꿈 수(1:1 대응).

```csv
"Question","Answer"
"이 과정은 반복되며
매 2분마다, 혹은 심지어 매 90초마다,
각 크레인은 시간당 30개에서 40개의 상자를 옮긴다
배에서 부두로.","The process is repeated
every two minutes, or even every ninety seconds,
each crane moving 30 or 40 boxes an hour
from ship to dock."
"그것이 혁명의 시작이었다.","Such was the beginning of a revolution."
```
엑셀에서 보이는 모습:

| Question | Answer |
|----------|--------|
| 이 과정은 반복되며⏎매 2분마다…⏎각 크레인은… | The process is repeated⏎every two minutes…⏎each crane… |

**방식 B (변형) — 한 청크 = 한 행**
문장으로 묶지 않고 청크 하나당 한 행으로 분리한다. "한 줄에 청크 하나씩" 요청 시 사용.

```csv
"Question","Answer"
"컨테이너가 타당하다고 할 때,","Given that containers made sense,"
"그것들은 얼마나 커야 할까?","how big should they be?"
```

> 두 방식은 배타적이다. 요청에 "한 줄에 하나씩"이 없으면 방식 A(문장 단위)가 기본.

### 분량이 많을 때
- 챕터/섹션 단위로 나눠 처리 (필요하면 병렬로)
- 최종적으로 하나의 통합 CSV로 병합 가능
- 분량이 많다고 거부하지 말 것 — 나눠서라도 전부 처리

---

## 스킬 2 — 단어장 예문 암기 CSV (워드리스트)

스킬 1의 변형. 단어장(단어 + 발음기호 + 품사 + 영어 정의 + 예문) 이미지를 받아 **단어별 예문 1행**으로 정리한다. 정의문은 버리지 않고 메모 열에 보존한다.

### 언제 쓰나
- 단어장/워드리스트 이미지를 주며 "정리해줘", "암기 CSV로", "단어장 번역" 요청 시

### 입력 예시 (이미지 한 항목)
```
acre [éikər]
n. An acre is a unit for measuring area.   ← 정의문
They lived on a 150-acre farm.             ← 예문
```

### 출력 형식 (3열)
| 열 | 내용 |
|----|------|
| A: Question | 한국어 청크 번역 (**예문** 기준, 영어 어순 1:1) |
| B: Answer | 영어 **예문** (정의문 제외, 청크별 줄바꿈) |
| C: Key Word | `단어 (품사약어)` + 영어 정의문 (줄바꿈으로 구분) |

### 행 규칙
- **1행 = 1단어** (예문 1개만 사용)
- 정의문은 B열에 넣지 않고 **C열 메모**로 보존
- C열 첫 줄 = `단어 (품사약어)` — 발음기호 제외, 품사는 괄호로 (`n.` `v.` `adj.` `adv.` 등)
- C열 둘째 줄 = 이미지의 영어 정의문 그대로

### 예시
```csv
"Question","Answer","Key Word"
"그들은 살았다
150에이커의 농장에서.","They lived
on a 150-acre farm.","acre (n.)
An acre is a unit for measuring area."
"나는 믿는다
사후세계가 있다고.","I believe
that there is an afterlife.","afterlife (n.)
The afterlife is a life that some people believe begins when a person dies."
```
엑셀에서 보이는 모습:

| Question | Answer | Key Word |
|----------|--------|----------|
| 그들은 살았다⏎150에이커의 농장에서. | They lived⏎on a 150-acre farm. | acre (n.)⏎An acre is a unit… |

### 작업 순서
1. 이미지에서 단어/품사/정의/예문 추출
2. **예문만** 의미 청크로 분할 (정의문은 C열로 보존)
3. 한국어 청크 번역 (영어 어순 1:1)
4. CSV 작성 (UTF-8 BOM, 따옴표, 실제 줄바꿈)
5. 여러 이미지·페이지면 같은 파일에 계속 누적

### 자주 헷갈리는 점
| 헷갈림 | 정답 |
|--------|------|
| 정의문도 B열(예문)에 넣나? | 아니. B열은 예문만. 정의문은 C열 메모. |
| 발음기호는? | 제외. C열엔 `단어 (품사)`만. |
| 한 단어를 정의/예문 두 행으로? | 아니. 1단어 = 1행 (예문 한 개만). |
| 품사 표기 위치? | 단어 뒤 괄호: `acre (n.)` |
| 단어와 정의 사이 구분? | 줄바꿈. 슬래시(`/`) 아님. |

---

## 빠른 요약

| | 스킬 1 (지문) | 스킬 2 (단어장) |
|--|--------------|----------------|
| 입력 | 영어 문단/책 텍스트 | 단어장 이미지 |
| 열 수 | 2열 (한/영) | 3열 (한/영 예문/단어·정의) |
| 행 단위 | 문장(기본) 또는 청크 | 단어 |
| 공통 | 영어 어순 1:1 · UTF-8 BOM · 셀 안 실제 줄바꿈 · 전 셀 따옴표 | ← 동일 |

> 결과 CSV는 엑셀/구글시트에서 열거나, 한국어=앞면·영어=뒷면 플래시카드로 임포트해 직독직해 반복 학습에 쓴다.
